# Frontend — Variables de entorno
# Copiar a .env.local para desarrollo

# URL base de la API (dejar vacío si se usa proxy de Vite)
VITE_API_URL=

# URL del WebSocket (dejar vacío si backend está en mismo origen)
VITE_WS_URL=
